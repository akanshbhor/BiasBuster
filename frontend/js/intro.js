document.addEventListener('DOMContentLoaded', () => {
    // Click anywhere on the page → redirect to Experience
    document.body.addEventListener('click', () => {
        window.location.href = 'experience.html';
    });
});
